{
  "_comment": "PLACEHOLDER. After Part A of onboarding/gsuite-setup.md, save the downloaded Desktop OAuth client here as oauth_client.json (without .example). It is GITIGNORED — never commit it; no secret belongs in git. The brain uploads it once to the private Brain Output Drive folder, and `gs.py login` auto-places it from each teammate's Downloads. Personal tokens also stay out of git (~/.shikshalokam/).",
  "installed": {
    "client_id": "REPLACE.apps.googleusercontent.com",
    "project_id": "shikshalokam-brain",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "REPLACE",
    "redirect_uris": ["http://localhost"]
  }
}
