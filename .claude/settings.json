{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then git add -A && git commit -m \"session: auto-commit at session end ($(date -u +%Y-%m-%dT%H:%M:%SZ))\"; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
