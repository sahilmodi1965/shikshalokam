{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git pull --ff-only origin main 2>&1 || true",
            "timeout": 20
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then git add -A && git commit -m \"session: auto-commit at session end ($(date -u +%Y-%m-%dT%H:%M:%SZ))\"; fi; git push origin main 2>&1 || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
